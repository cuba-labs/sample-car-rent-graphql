type Query {
    scr_CarById(id: String): scr_Car
    scr_Cars: [scr_Car]
    scr_GarageById(id: String): scr_Garage
    scr_Garages: [scr_Garage]
}

type scr_Car {
    garage: scr_Garage
    id: ID!
    regNumber: String
}

type scr_Garage {
    cars: [scr_Car]
    id: ID!
    name: String
}