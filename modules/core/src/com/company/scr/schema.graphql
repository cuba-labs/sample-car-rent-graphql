schema {
  query: Query
}

# Main query type for fetching data from tables
type Query {
  cars: [Car]
  garages: [Garage]
}

type Car {
  id: ID!,
  regNumber: String
  garage: Garage
}

type Garage {
  id: ID!,
  name: String,
  cars: [Car]
}
