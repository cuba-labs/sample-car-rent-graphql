schema {
  query: Query
}

# Main query type for fetching data from tables
type Query {
  cars(regNumber: String): [Car]
  garages(name: String): [Garage]
}

type Car {
  id: ID!,
  regNumber: String
  garage: Garage
}

type Garage {
  id: ID!,
  name: String,
  cars: [Car]
}
